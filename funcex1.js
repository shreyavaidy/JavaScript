function min(num1, num2)
{   console.log("The smaller number between "+num1+" and "+num2+" is :");
    var small=0;
    if(num1<num2)
        small=num1;
    else
        small=num2;
    console.log(small);
};

min(15,10);
